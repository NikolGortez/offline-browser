// preload.js
// (на самом деле можно оставить пустым, если не нужен bridge)
window.addEventListener('DOMContentLoaded', () => {
  // Можно здесь экспортировать из процесса Node какие-то константы в window
});
